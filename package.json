{
  "name": "patient-portal",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.16.3",
    "fhir-kit-client": "^1.0.0"
  },
  "scripts": {
    "start": "concurrently \"cd server && nodemon server\" \"cd frontend && npm start\"",
    "install": "cd frontend/ && npm install && cd ../server/ && npm install",
    "heroku-postbuild": "cd frontend && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  }
}
